{% extends "AppBundle::base.html.twig" %}


{% block title %}iFarm - pro moderní farmáře{% endblock %}

{% block body %}
    <div class="container">

        <div class="row" style="line-height: 130px;">
            <div class="col s12 m4 ">
                <h1 class="center-align">Skupiny</h1>
            </div>
        </div>

        {% for gathering in gatherings %}
            <a class="waves-effect waves-light btn" href="{{ path('main_gathering',{gathering : gathering.id} )}}">{{gathering.name}}</a>
        {% endfor %}


    </div>

{% endblock %}
